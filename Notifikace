name: Notifikace změny

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check_changes:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Check webpage and notify
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        # Zde bude následovat skript pro kontrolu změny
